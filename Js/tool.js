const testFolder = './assests/';
const fs = require('fs');

let arr= fs.readdirSync(testFolder).forEach(file => {
  // console.log(file)
});

 let Array =fs.readdirSync(testFolder);
 console.log(Array)


 

